services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - ./src:/usr/share/nginx/html
    environment:
      - APP_ENV=dev
      - APP_KOMENTAR=Nalazimo se u dev okruzenju
