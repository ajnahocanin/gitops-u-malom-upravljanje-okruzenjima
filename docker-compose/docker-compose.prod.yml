services:
  app:
    build: .
    ports:
      - "80:80"
    environment:
      - APP_ENV=prod
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M
